<h1><%= @book.first.facility %></h1>
<h3>DANH SACH</h3>
<div class="col-md-10 col-md-offset-1">	
	<ul class="list-group">
	<% @book.each do |book| %>
	<li class="list-group-item">
		<b><%= link_to book.title, home_show_path(id: book.id) %></b>
		
	</li>
	<% end %>
	</ul>
	<p style="font-size: 1.3em"><span class="glyphicon glyphicon-hand-left"><%= link_to "Back", :back %></p>
</div>